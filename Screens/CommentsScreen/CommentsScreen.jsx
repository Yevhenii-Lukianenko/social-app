import { StyleSheet, View } from "react-native";

const CommentsScreen = () => <View style={styles.container}></View>;

export default CommentsScreen;

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: "#fff" },
});
