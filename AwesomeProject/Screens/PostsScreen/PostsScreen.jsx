import { StyleSheet, View } from "react-native";

export const PostsScreen = () => <View style={styles.postsContainer}></View>;

const styles = StyleSheet.create({
  postsContainer: { flex: 1, backgroundColor: "#FFF" },
});
